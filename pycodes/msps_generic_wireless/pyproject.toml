[project]
name = "msps_generic_wireless"
version = "0.1.0"
description = "妙算管道阶段模型 generic_wireless"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "miaosuan"
]

[build-system]
requires = ["uv_build>=0.9.4,<0.10.0"]
build-backend = "uv_build"
